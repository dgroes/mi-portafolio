---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---
<!-- C06: Section Container -->

<section
  id={id}
  class:list={[
    "w-full max-w-5xl mx-auto px-4 sm:px-6 lg:px-4 py-6 sm:py-8",
    className,
  ]}
>
  <slot />
</section>
